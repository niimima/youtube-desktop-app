<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="PlaylistEditor.Views.SearchFormView">
	<StackPanel>
		<StackPanel Margin="5"
					Orientation="Horizontal">
			<TextBlock VerticalAlignment="Center" Margin="10">Search : </TextBlock>
			<TextBox Name="m_SearchWordTextBox" Height="20" Width="280" Watermark="Search Word"
					 Text="{Binding Path=SearchWord.Value}">
				<TextBox.KeyBindings>
					<KeyBinding Gesture="Enter" Command="{Binding Search}"/>
				</TextBox.KeyBindings>
			</TextBox>
		</StackPanel>
		<StackPanel Margin="5" >
			<TextBlock VerticalAlignment="Center" Margin="10">Search Option: </TextBlock>
			<TextBox Margin="10" Height="100" Width="320" Watermark="Search Option"
					 IsEnabled="False"/>
		</StackPanel>
		<Button HorizontalAlignment="Right" Margin="5" Command="{Binding Search}">
			Search
		</Button>
	</StackPanel>
</UserControl>
